name: 'oortclouds-docs'

on:
  push:
    branches:
      - master

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@master
      - name: 'Start Work'
        run: yarn docs:build
