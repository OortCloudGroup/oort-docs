name: 'GitHub Actions Weather Bot'

on:
  push:

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
      - name: 'Start Work'
        run: docs:build
